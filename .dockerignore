
# config.yaml: ホスト側のファイルを利用するため不要
config.yaml

# Git metadata: バージョン判定は実行時の read-only source tree bind mount を利用する
.git/

# node_modules: ファイル数が多すぎてコピーに時間がかかる
client/node_modules/

# ホスト側の既存 build 成果物: Docker 内で再生成する
client/dist/

# poetry の仮想環境: Docker 上で新たに作り直すため不要
server/.venv/

# Python のテスト・実行時キャッシュ: ホスト環境の生成物をコンテナへ持ち込まない
**/__pycache__/
**/*.pyc
**/*.pyo
**/.pytest_cache/
**/.ruff_cache/

# データベース: マイグレーションに失敗するのを防ぐ
server/data/*

# サーバーログ: ホスト側の既存ログを build context へ含めない
server/logs/*

# ACP / Google のホスト資格情報は配置場所にかかわらず build context へ含めない
**/.codex/
**/.grok/
**/.config/gcloud/
**/auth.json
**/application_default_credentials.json

# サードパーティーライブラリ: Docker 側でファイルを準備するため不要
server/thirdparty/

# tsreadex: Docker の builder stage でコンテナ環境向けにビルドする
thirdparty-src/tsreadex/build/
thirdparty-src/tsreadex/tsreadex
thirdparty-src/tsreadex/tsreadex-anchor-test
thirdparty-src/tsreadex/tsreadex.elf
