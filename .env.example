# ホストのユーザー・GPUデバイスに合わせて変更してください。
KONOMITV_UID=1000
KONOMITV_GID=1000
KONOMITV_VIDEO_GID=44
KONOMITV_RENDER_GID=992

# 使用する GPU にあわせてどれか一つをコメントアウトを外してください
#GPUなし
COMPOSE_FILE=compose.yaml
#Intel・AMD GPU
#COMPOSE_FILE=compose.yaml:compose.intel-amd.yaml
#NVIDIA GPU
#COMPOSE_FILE=compose.yaml:compose.nvidia.yaml
#Intel・AMD と NVIDIA GPU の併用
#COMPOSE_FILE=compose.yaml:compose.intel-amd.yaml:compose.nvidia.yaml

# Dockerイメージへ組み込むCUDAと非自由コンポーネントの構成です。
# RTX50以降の場合は12.8指定
KONOMITV_CUDA_VERSION=12.4
# 非自由コンポーネントの構成（nonfree / free / intel-nonfree / amd-nonfree）
# nonfree:       Intel 非自由カーネルとAMD proprietary runtimeの両方を含める（Intel/AMD HEVC HWENCODEが可能。再配布不可）
# intel-nonfree: Intel 非自由カーネルのみを含める（Intel HEVC HWENCODEが可能。再配布不可）
# amd-nonfree:   AMD proprietary runtimeのみを含める（AMD HEVC HWENCODEが可能。再配布不可）
# free:          非自由コンポーネントを含めない（再配布可能。Intel/AMD の HEVC HWENCODEは不可。NVIDIA はホスト側ランタイムで利用可）
# 互換のため true（=nonfree）/ false（=free）も受け付けます
KONOMITV_NONFREE=nonfree

# Google Vertex AIを利用する場合に設定してください。
GOOGLE_VERTEX_PROJECT=
GOOGLE_VERTEX_LOCATION=global
